# HP Omen TU116 desktop path.
#
# Mutter reported nouveau running in non-atomic KMS mode. Prefer atomic KMS at
# module load so GNOME can avoid the older non-atomic presentation path when
# the kernel/driver combination supports it.
options nouveau atomic=1
